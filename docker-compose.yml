services:
  books-site:
    container_name: books-site
    image: ghcr.io/chris-shaw-2011/books:latest
    pull_policy: always
    environment:
      - NODE_ENV=production
    ports:
      - "3001:3001"
    restart: always
